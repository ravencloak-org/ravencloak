VITE_KEYCLOAK_URL=http://localhost:8088
VITE_KEYCLOAK_REALM=master
VITE_KEYCLOAK_CLIENT_ID=kos-admin-web
# Leave empty for local dev — Vite proxy forwards /api to localhost:8080
# Only set when the backend runs on a different origin (e.g. http://localhost:8091 in Docker)
VITE_API_BASE_URL=
VITE_OTEL_ENABLED=false
VITE_OTEL_COLLECTOR_URL=http://localhost:4318
