# Auth Backend - Build & Test (Push/PR/Manual)
when:
  - event: [push, pull_request, manual]
    path:
      include:
        - "src/**"
        - "build.gradle.kts"
        - "settings.gradle.kts"
        - ".woodpecker/auth*.yml"
      exclude:
        - "keycloak-spi/**"

steps:
  - name: build
    image: eclipse-temurin:21-jdk
    commands:
      - echo "Building Auth Backend..."
      - ./gradlew compileKotlin bootJar --no-daemon --build-cache -x :keycloak-spi:compileKotlin -x test
      - echo "Build successful!"
    volumes:
      - /opt/woodpecker-cache/gradle:/root/.gradle
