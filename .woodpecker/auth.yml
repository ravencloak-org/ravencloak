# Auth Backend - Build & Test (Push/PR/Manual)
# Manual trigger: woodpecker-cli pipeline create dsjkeeplearning/kos-auth-backend --branch main --var DEPLOY_TO=auth-build
when:
  - event: [push, pull_request]
  - event: manual
    evaluate: 'CI_PIPELINE_DEPLOY_TO == "auth-build"'
    path:
      include:
        - "src/**"
        - "build.gradle.kts"
        - "settings.gradle.kts"
        - ".woodpecker/auth*.yml"
      exclude:
        - "keycloak-spi/**"

steps:
  - name: build
    image: eclipse-temurin:21-jdk
    volumes:
      - /var/lib/woodpecker/cache/gradle:/root/.gradle
    environment:
      CI: "true"
      # Daemon JVM args are set in gradle.properties (org.gradle.jvmargs)
      # GRADLE_OPTS only affects the client launcher JVM, not the build daemon
      GRADLE_OPTS: "-Xmx128m"
      S3_BUILD_CACHE_BUCKET:
        from_secret: s3_build_cache_bucket
      S3_BUILD_CACHE_REGION:
        from_secret: s3_build_cache_region
      AWS_ACCESS_KEY_ID:
        from_secret: s3_build_cache_access_key_id
      AWS_SECRET_ACCESS_KEY:
        from_secret: s3_build_cache_secret_key
      S3_BUILD_CACHE_ENDPOINT:
        from_secret: s3_build_cache_endpoint
    commands:
      - echo "Building Auth Backend..."
      - find /root/.gradle -name "*.lock" -type f -delete 2>/dev/null || true
      # Split into separate invocations to reduce peak memory per phase
      # Build cache ensures tasks aren't re-executed across invocations
      - ./gradlew compileKotlin compileTestKotlin --no-daemon --max-workers=1 -x :keycloak-spi:compileKotlin
      - ./gradlew test --no-daemon --max-workers=1 -x :keycloak-spi:test
      - ./gradlew bootJar --no-daemon --max-workers=1
      - echo "Build successful!"
